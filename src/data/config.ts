export const GAME_CONFIG = {
    RENDER: {
        FPS_LIMIT: 60,
    },
    MAP: {
        NOISE: {
            LUSH_THRESHOLD: 1.2,
            FLOWERS_THRESHOLD: 0.5,
            SPARSE_THRESHOLD: -1.0,
        }
    },
    LOOP: {
        TICK_RATE: 50,
        MAX_ACCUMULATOR: 500,
    },
    ENTITIES: {
        ENEMY_BASE_SPEED: 350,
    }
};
